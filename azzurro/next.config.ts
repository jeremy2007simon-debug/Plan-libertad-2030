import type { NextConfig } from "next";

// Desplegado en Vercel con "Root Directory" = azzurro (proyecto separado
// del resto de webs de este repositorio).
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
