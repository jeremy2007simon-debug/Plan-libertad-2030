import type { NextConfig } from "next";

// Desplegado en Vercel con "Root Directory" = amigos-del-norte (proyecto
// separado de la web de NovaCore, que vive en la raíz de este repo).
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
